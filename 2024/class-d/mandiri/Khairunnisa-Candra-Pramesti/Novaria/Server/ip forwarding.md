# IP FORWARDING (asccinema 5)


```
ssh nayla@10.219.56.6
```

```
sudo systemctl enable --now systemd-networkd
nvim /etc/sysctl.d/30-ipforward.conf
sudo nvim /etc/sysctl.d/30-ipforward.conf
sudo sysctl –system
```
## LAST STEPS
```
Logout
exit
```
