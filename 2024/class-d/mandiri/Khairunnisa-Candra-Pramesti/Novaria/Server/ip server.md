# IP SERVER (asciinema 4)

```
nvim /etc/systemd/network/20-ethernet.network
sudo nvim /etc/systemd/network/20-ethernet.network
```

```
ssh nayla@10.219.56.6
iwctl
nmtui
ssh nayla@10.219.56.6
sudo nvim /etc/systemd/network/20-ethernet.network
```

```
pacman -S hostapd
sudo pacman -S hostapd
sudo nvim /etc/hostapd/hostapd.conf
sudo nvim /etc/systemd/network/02-wireless-ap.networks
```

```
systemctl restart systemd-networkd
systemctl restart systemd-networkd
systemctl restart systemd-networkd
systemctl restart systemd-networkd
systemctl restart systemd-networkd
```

## LAST STEPS IS...
```
exit
```

Jan lupa ctrl+d
upload asciinema .cast



